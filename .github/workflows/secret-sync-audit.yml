name: Secret Sync Audit

# Plan v3.1 §D.4 / §D.6. Runs the secret-sync-audit.sh script on PR,
# on a daily schedule, and on-demand. Catches drift between Infisical,
# wrangler staging, and wrangler prod planes via hash comparison (no
# values ever cross the plane boundary in cleartext).

on:
  pull_request:
    branches: [main]
    paths:
      - 'scripts/secret-sync-audit.sh'
      - 'scripts/sync-shared-secrets.sh'
      - 'workers/crane-context/src/endpoints/admin-secret-hash.ts'
      - '.github/workflows/secret-sync-audit.yml'
  schedule:
    # Daily at 09:00 MST (16:00 UTC)
    - cron: '0 16 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    name: Secret Sync Audit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'

      - name: Install Infisical CLI
        run: |
          curl -1sLf 'https://dl.cloudsmith.io/public/infisical/infisical-cli/setup.deb.sh' | sudo -E bash
          sudo apt-get update
          sudo apt-get install -y infisical

      - name: Authenticate Infisical (machine identity)
        env:
          INFISICAL_UNIVERSAL_AUTH_CLIENT_ID: ${{ secrets.INFISICAL_UNIVERSAL_AUTH_CLIENT_ID }}
          INFISICAL_UNIVERSAL_AUTH_CLIENT_SECRET: ${{ secrets.INFISICAL_UNIVERSAL_AUTH_CLIENT_SECRET }}
        run: |
          if [ -z "$INFISICAL_UNIVERSAL_AUTH_CLIENT_ID" ] || [ -z "$INFISICAL_UNIVERSAL_AUTH_CLIENT_SECRET" ]; then
            echo "::warning::Infisical machine identity not configured — skipping audit"
            echo "INFISICAL_SKIP=1" >> $GITHUB_ENV
            exit 0
          fi
          infisical login \
            --method=universal-auth \
            --client-id="$INFISICAL_UNIVERSAL_AUTH_CLIENT_ID" \
            --client-secret="$INFISICAL_UNIVERSAL_AUTH_CLIENT_SECRET" \
            --silent

      - name: Run secret-sync-audit (hash + rotation-age)
        if: env.INFISICAL_SKIP != '1'
        run: |
          bash scripts/secret-sync-audit.sh --mode=all --ci
