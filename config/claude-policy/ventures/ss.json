{
  "autoMode": {
    "soft_deny": [
      "$defaults",
      "Fly Operator Seats [named+specifics — **must name:** the Fly app and the operation]: The Fly apps `hermes-ashton-price` and `hermes-pilot-smokeball` are live client Operator runtimes reached via `fly`/`flyctl`; `hermes-ashton-price` serves a paying client in production. Their volume `/opt/data` holds profile homes, cron stores, and tokens that survive reprovisioning, so recreating an app does not recreate its state. Treat as production for both deploy and read rules: deploying, restarting, stopping, scaling, destroying or deleting the app, machines or volumes (`destroy`, `delete`, `rm` spellings alike), changing secrets, or opening a shell or file transfer into them (`fly ssh console`, `fly ssh sftp`) is a production action. Streaming or dumping their logs is a Production Read — output may carry client PII and tokens into the transcript. Clears only when the user's message names that specific app and that specific operation."
    ],
    "environment": [
      "### Venture overlay — SMD Services (ss)",
      "**Venture**: SMD Services, a consulting venture under SMDurgan, LLC. This session's repo is `github.com/venturecrane/ss-console` (local path `~/dev/ss-console`). The facts below apply to this venture only and do not generalise to sibling ventures.",
      "**Repository visibility**: `venturecrane/ss-console` is **public** (verified via `gh repo view`: `\"visibility\":\"PUBLIC\"`, `\"isPrivate\":false`). Only this repo's own work belongs here — content ported, repointed, or first read from outside this repo is not its own work, whoever directed the port. Client and engagement material (dossiers, correspondence, proposals, agreements) belongs in the private sibling `venturecrane/engagements` and must never be committed here.",
      "**Venture cloud footprint**: the `ss-web` Cloudflare Worker (Workers + Static Assets, D1, R2, KV) and Fly.io apps `hermes-ashton-price.fly.dev` / `hermes-pilot-smokeball.fly.dev` — the Operator runtime. `fly`/`flyctl` is used heavily in this venture; `operator` is its own CLI. Sentry project `smd-operator` via `mcp.sentry.dev/mcp/smdurgan-llc`.",
      "**Venture secrets path**: Infisical `--env=prod --path=/ss` is this venture's production secret path; `ss-web` also carries Cloudflare Worker secrets. Repo docs reference an export of that path piped into a bulk Worker secret upload — the pipe is the supported form; the value must never land in a transcript.",
      "**CI/CD deploy targets**: Cloudflare Worker `ss-web` via `wrangler deploy`, serving `smd.services` (marketing, public), `admin.smd.services` (admin console), and `portal.smd.services` (client portal); a staging build/deploy exists (`npm run build:staging` / `deploy:staging`) and is not protected. Fly.io `hermes-*` apps deployed via `fly`/`flyctl`. CI secrets referenced: AGENTMAIL_API_KEY, CLOUDFLARE_*, CRANE_RELAY_KEY, GITHUB_TOKEN, OPERATOR_RUNTIME_READ_*.",
      "**Trusted internal domains**: `smd.services` and its subdomains (`*.smd.services`) — this venture's own deployment. Third-party APIs seen in this venture (`login.microsoftonline.com`, `graph.microsoft.com`, `api.smokeball.com`, `api.agentmail.to`, `api.anthropic.com`, `mcp.sentry.dev`) are vendors, not internal.",
      "**Venture key services**: the `operator` CLI; the Operator runtime on Fly.io (`hermes-ashton-price.fly.dev`, `hermes-pilot-smokeball.fly.dev`); the `ss-web` Cloudflare Worker and its D1/R2/KV bindings.",
      "**Venture sensitive data locations**: client and engagement material (dossiers, correspondence, proposals, agreements) lives in the private sibling repo `venturecrane/engagements` under `operator/customers/<slug>/` and must never be written into `ss-console`; the D1 admin data (clients, assessments, quotes, engagements, billing) behind `admin.smd.services`; `operator/customers/<slug>/customer.yaml` and `routine-grid.yaml`; credential-custody code and migrations (`src/lib/operator/credential-*`, `migrations/0050_*`, `0051_*`); Infisical `--env=prod --path=/ss`; the Fly volume `/opt/data` (profile homes, cron stores, tokens). Client-facing content must come only from engagement-authored data. Captain contact is `scott@smd.services`, operational alerts `team@smd.services`; `smdurgan@venturecrane.com` belongs to a different venture and must not appear in SMD material.",
      "**Venture sensitive remote targets**: the live `ss-web` Worker and its domains (`smd.services`, `admin.smd.services`, `portal.smd.services`), the Cloudflare D1/R2/KV bindings behind it, Infisical `env=prod` at `/ss`, the Fly apps `hermes-*.fly.dev` (Operator runtime serving real clients, volume `/opt/data`), and the `operator` k8s namespace (seen with `-n operator`).",
      "**Venture protected environments**: the production `ss-web` Cloudflare Worker deployment (as opposed to the staging build), Infisical `prod` at `/ss`, the Fly.io Operator apps (`hermes-*`), and the `operator` namespace. `deploy:staging` and `localhost:4321` are not protected.",
      "**Venture protected IaC scopes**: `wrangler.toml` bindings and custom-domain routes for `ss-web`, Cloudflare DNS for `smd.services`, and `fly.toml` / Fly volume and secret configuration for the `hermes-*` apps.",
      "**Venture routine**: session mission/board state under `~/.claude/ss-board/`; the engagements checkout at `~/dev/engagements` (or `SS_ENGAGEMENTS_DIR`) is read for Law 2 gating but is a separate private repo, not this session's trusted repo."
    ]
  }
}
